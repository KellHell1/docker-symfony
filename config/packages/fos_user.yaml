fos_user:
  db_driver: orm # other valid values are 'mongodb' and 'couchdb'
  firewall_name: main
  user_class: www\Entity\User
  from_email:
    address: "test@domain.com"
    sender_name: "test@domain.com"
  service:
    mailer: 'fos_user.mailer.noop'
